<%= form_with(model: @cost_center, id: 'form-cost_center', class: 'needs-validation g-brd-around g-brd-gray-light-v4 g-pa-10 g-mb-10') do |form| %>
  <div class="form-group row">
    <%= form.label :function_id, 'Función:' ,class:'col-form-label col-sm-3 col-md-4' %>
    <div class="col-sm-8 col-lg-6">
      <%= form.select(:function_id, Function.actives.collect { |e| ["#{e.code}-#{e.name}", e.id] }, 
        { include_blank: true},
        required: true,
        class: 'form-control select-2-function') %>
    <small class="form-control-feedback text-danger "></small>
    </div>
  </div>
 <div class="form-group row">
    <%= form.label :unit_business_id, 'Unidad de negocios:' ,class:'col-form-label col-sm-3 col-md-4' %>
    <div class="col-sm-8 col-lg-6">
      <%= form.select(:unit_business_id, UnitBusiness.actives.collect { |e| ["#{e.code}-#{e.name}", e.id] }, 
        { include_blank: true},
        required: true,
        class: 'form-control select-2-unit-business') %>
    <small class="form-control-feedback text-danger "></small>
    </div>
  </div>

  <div class="actions">
    <%= form.submit 'Guardar', class: 'btn u-btn-primary mr-2' %>
    <button type="button" class="btn u-btn-red" data-dismiss="modal">Cerrar</button>
  </div>
<% end %>
