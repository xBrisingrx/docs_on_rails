<% if current_user.admin? %>
  <%= link_to "<i class='fa fa-plus'></i> Registrar zona".html_safe, 
        new_zone_path( d_type: params[:d_type] ),
        remote: true, 
        class: 'btn u-btn-outline-indigo u-btn-hover-v1-2 mb-2' %>
<% end %>

<%= render( partial: 'shared/table', 
            locals: { table_id: 'zones', 
                      color: 'blue', 
                      icon: 'fa-map',
                      title: "Zonas registradas para #{@type}", 
                      col_names: [ 'Código','Nombre', 'Descripcion', 'Acciones' ] }) %>